[build]
builder = "nixpacks"
buildCommand = "npm run build"

[deploy]
startCommand = "mkdir -p /app/data && npx prisma migrate deploy && npm start"
healthcheckPath = "/"
healthcheckTimeout = 300

[[volumes]]
name = "sqlite_data"
mountPath = "/app/data"
